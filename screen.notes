

Recuerda que para crear una session:

screen -S TEST

y para crear nuevas ventanas en la session:

screen -r TEST -X screen -t newwindow

para moverte entre ventanas

screen -r TEST -X select [0,1...9]

y lo demás ya lo sabes...

